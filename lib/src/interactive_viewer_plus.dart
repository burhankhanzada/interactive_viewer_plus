import 'package:flutter/material.dart';

class InteractiveViewerPlus extends StatelessWidget {
  const InteractiveViewerPlus({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
